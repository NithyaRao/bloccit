<h1>New Question</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for Asking Questions</p>
     <ul>
       <li>Make sure it's clear and appropriate.</li>
       <li>Be descriptive </li>
       <li> Resolved,lets other users know when answers are no longer needed. </li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @question do |q| %>
       <div class="form-group">
         <%= q.label :title %>
         <%= q.text_field :title, class: 'form-control', placeholder: "Enter Question" %>
       </div>
       <div class="form-group">
         <%= q.label :body %>
         <%= q.text_area :body, rows: 8, class: 'form-control', placeholder: "Question Body " %>
       </div>
       <div class="form-group">
        <%= q.label "Resolved " %> 
        <%= q.check_box :resolved %> 
       </div>
       <div class="form-group">
         <%= q.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>